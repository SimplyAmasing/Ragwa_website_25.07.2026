node_modules
dist
.env
.env.local
.figma
!.figma/
!.figma/make/
!.figma/make/site.json

# Local UI-review only: full-resolution product photos used by the mock catalog.
# Real catalog images are served from R2 via the Worker.
public/mock-images/
