packages:
  - '.'

# Dependency lifecycle scripts pnpm is allowed to run on install. Firebase and
# its transitive protobufjs ship build scripts that the Vite bundle does not
# need, so none are enabled.
onlyBuiltDependencies: []
